# Evals — Case Filing Ai

- **datasets/** — fixtures (input, expected constraints).
- **runners/** — `*.eval.mjs` files executed via `npm run test:evals`.

Run: `npm run test:evals -- case-filing-ai`
