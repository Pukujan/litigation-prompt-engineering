# File exchange

Maintenance API for `file-exchange/imports` and `file-exchange/exports` (dated session folders).

**CLI:** `npm run clear:file-exchange` (from repo root)

**API:** `POST /api/file-exchange/clear` — see [docs/file-exchange/API.md](../../../docs/file-exchange/API.md)
