# Consolidated files (latest mirror)

Regenerated by `npm run condense:all` (or individual condense scripts).

Same JSON as `file-exchange/exports/consolidated-*.json` and dated `exports/{stamp}_consolidated/` folders.

| File | Source |
|------|--------|
| `consolidated-models.json` | `npm run condense-models` / model-condenser API |
| `consolidated-prompts.json` | `npm run condense-prompts` |
| `consolidated-file-structure.json` | `npm run condense-file-structure` |
